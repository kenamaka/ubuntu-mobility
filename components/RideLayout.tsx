import React from "react";
import { Text, View } from "react-native";

const RideLayout = ({ children }: { children: React.ReactNode }) => {
  return (
    <View>
      <Text>Ride Layout</Text>
    </View>
  );
};

export default RideLayout;
